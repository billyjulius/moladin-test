{
  "name": "moladin-test",
  "version": "1.0.0",
  "description": "Automation code for Moladin SDET interview test",
  "author": "Billy Julius",
  "scripts": {
    "cypress-open": "cypress open",
    "web-test": "cypress run --browser chrome",
    "web-test-firefox": "cypress run --browser firefox --headed",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cypress": "^9.6.0",
    "cypress-iframe": "^1.0.1",
    "eslint-plugin-cypress": "^2.12.1"
  }
}
